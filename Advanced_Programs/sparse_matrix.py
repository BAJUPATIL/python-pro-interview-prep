# Program to represent a sparse matrix efficiently.
# A sparse matrix is a matrix in which most elements are zero.
# Example: Representing matrix [[0, 0, 3], [0, 0, 0], [0, 5, 0]] using sparse matrix format.
