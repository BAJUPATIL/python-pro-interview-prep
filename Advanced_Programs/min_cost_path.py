# Program to find the minimum cost path in a matrix.
# Minimum cost path is the least cost path from the top-left corner to the bottom-right corner.
# Example: Path from (0, 0) to (n-1, m-1) with minimal cost.
