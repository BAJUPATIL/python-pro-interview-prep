# Program to implement an LRU (Least Recently Used) cache.
# LRU cache stores a fixed number of items and evicts the least recently used item when full.
# Example: Using an LRU cache to store recent API results.
