# Program to implement a disjoint set (union-find) data structure.
# Disjoint set is used for tracking a partition of a set into disjoint subsets.
# Example: Union and find operations on sets of elements.
